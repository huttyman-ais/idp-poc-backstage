# ${{values.appName}}-frontend

React + TypeScript frontend for **${{values.appName}}**, generated by the Backstage
`create-application` template.

- **Owner:** ${{values.owner}}
- **Team:** ${{values.teamName}}
- **Environment:** ${{values.environment}}

## Local development

```bash
npm install
npm run dev
```

## Deployment

Deploys automatically on push to `main` via `.github/workflows/ci-cd.yaml` — builds a container
image, pushes to Azure Container Registry, and updates the Azure Container App
`${{values.appName}}-frontend`.
